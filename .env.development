# Development environment defaults (committed)
# For local overrides, create .env.local or .env (both gitignored)

# Enable tracing output in investigation tests
# Usage: just investigate-llm tree_agent_
TEST_LOG=1

# Eval/runner logging (change to debug for verbose output)
RUST_LOG=eval=info,runner_ui=info
